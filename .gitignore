# Node / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vite / build output
game/dist/
game/.vite/
game/.cache/
*.local

# 编辑器 / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
desktop.ini

# Claude 工作目录（不应进版本控制）
.claude/

# 测试 / 临时
*.log
*.tmp
coverage/
.env
.env.local

# ── 项目门面瘦身（2026-06-17）· 原始素材 / 历史存档 / 构建产物移出版本库 ──
# 经 git rm --cached 停止追踪：本地文件原样保留、git 历史亦未重写，三重安全。
# 仅为保持 showcase 仓库轻量；游戏运行所需成品资源均在 game/src/assets/。
/asset/
/assetofsanguo/
/release/

# 对话原始记录备份（149 MB，太大不入 git）
docs/11-对话原始记录-*.jsonl

# 未压缩原始资源备份（296 MB，本地保留供未来打安装包用，不入 git）
# 任何时候可从 git tag v5.5-uncompressed-backup 还原
game/_assets_original/

# 样品对比目录
game/_compress_samples/

# WebP 转换前原 PNG 备份（仅本地保留，方便回滚）
backup-original-png/

# 压缩对比预览（开发者参考用）
compression-preview/

# 临时调试文件
_tmp_*.txt
preview-*.html
onl.py
" -*"
